# ========================================
# BLOCKCHAIN FIX DEPLOYMENT
# ========================================
# This fixes the blockchain section to use the correct port

# Copy and paste these commands into your EC2 terminal:

cd ~/corechain-build
git pull origin main
sudo docker cp ~/corechain-build/dashboard/index.html $(sudo docker ps -q -f name=aggregator):/var/www/html/index.html
echo ""
echo "✅ Blockchain fix deployed!"
echo "Clear browser cache (Ctrl+Shift+R) and refresh: http://54.173.119.88"
echo ""
echo "Expected dashboard sections:"
echo "  ✅ Training Status: running, Round 2/10, Progress 20%"
echo "  ✅ Hospitals: FL Test Hospital (connected)"
echo "  ✅ Blockchain Stats: 1 Block, 1 Transaction, Valid Chain"
echo "  ℹ️  Recent Transactions: Hidden (endpoint not available)"
